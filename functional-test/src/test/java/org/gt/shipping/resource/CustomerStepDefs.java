package org.gt.shipping.resource;

import cucumber.api.java8.En;

public class CustomerStepDefs implements En {
    public CustomerStepDefs() {
        Given("^resource intends to login with the application using valid credentials$", () -> {

        });
    }
}
