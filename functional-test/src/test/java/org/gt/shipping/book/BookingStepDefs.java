package org.gt.shipping.book;

import cucumber.api.PendingException;
import cucumber.api.java8.En;
import org.gt.shipping.BaseFunctionalTest;

public class BookingStepDefs  extends BaseFunctionalTest implements En{
    public BookingStepDefs() {
        Given("^a resource intends to book a cargo$", () -> {
            // Write code here that turns the phrase above into concrete actions
            throw new PendingException();
        });

        When("^the resource attempts to book a cargo$", () -> {
            // Write code here that turns the phrase above into concrete actions
            throw new PendingException();
        });
    }
}
